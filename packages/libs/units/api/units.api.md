## API Report File for "@squawk/units"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

declare namespace altitude {
    export {
        feetToMeters_2 as feetToMeters,
        metersToFeet_2 as metersToFeet,
        AltitudeUnit
    }
}

// @public
type AltitudeUnit = 'ft' | 'm';

declare namespace angle {
    export {
        degreesToRadians,
        radiansToDegrees,
        AngleUnit
    }
}

// @public
type AngleUnit = 'deg' | 'rad';

// @public
function celsiusToFahrenheit(celsius: number): number;

// @public
function celsiusToKelvin(celsius: number): number;

// @public
function degreesToRadians(degrees: number): number;

// @public
function densityAltitudeFt(pressureAltitudeFt: number, oatCelsius: number): number;

declare namespace distance {
    export {
        nauticalMilesToKilometers,
        kilometersToNauticalMiles,
        nauticalMilesToStatuteMiles,
        statuteMilesToNauticalMiles,
        nauticalMilesToMeters,
        metersToNauticalMiles,
        nauticalMilesToFeet,
        feetToNauticalMiles,
        kilometersToStatuteMiles,
        statuteMilesToKilometers,
        kilometersToMeters,
        metersToKilometers,
        kilometersToFeet,
        feetToKilometers,
        statuteMilesToMeters,
        metersToStatuteMiles,
        statuteMilesToFeet,
        feetToStatuteMiles,
        feetToMeters,
        metersToFeet,
        DistanceUnit
    }
}

// @public
type DistanceUnit = 'nm' | 'sm' | 'km' | 'm' | 'ft';

// @public
function fahrenheitToCelsius(fahrenheit: number): number;

// @public
function fahrenheitToKelvin(fahrenheit: number): number;

// @public
function feetToKilometers(feet: number): number;

// @public
function feetToMeters(feet: number): number;

// @public
function feetToMeters_2(feet: number): number;

// @public
function feetToNauticalMiles(feet: number): number;

// @public
function feetToStatuteMiles(feet: number): number;

declare namespace format {
    export {
        isFlightLevel,
        formatFlightLevel,
        formatAltitude,
        formatSpeed,
        formatQNH,
        formatDistance,
        formatFuel,
        formatTemperature,
        FormatSpeedUnit,
        FormatOptions,
        FormatTemperatureOptions
    }
}

// @public
function formatAltitude(altitudeFt: number, options?: FormatOptions): string;

// @public
function formatDistance(value: number, unit: DistanceUnit, options?: FormatOptions): string;

// @public
function formatFlightLevel(altitudeFt: number): string;

// @public
function formatFuel(value: number, unit: FuelUnit, options?: FormatOptions): string;

// @public
interface FormatOptions {
    locale?: string;
    precision?: number;
}

// @public
function formatQNH(value: number, unit: PressureUnit, options?: FormatOptions): string;

// @public
function formatSpeed(value: number, unit: FormatSpeedUnit, options?: FormatOptions): string;

// @public
type FormatSpeedUnit = 'kt' | 'km/h' | 'mph' | 'm/s' | 'mach';

// @public
function formatTemperature(value: number, unit: TemperatureUnit, options?: FormatTemperatureOptions): string;

// @public
interface FormatTemperatureOptions extends FormatOptions {
    altitudeFt?: number;
    showISADeviation?: boolean;
}

declare namespace fuel {
    export {
        gallonsToLiters,
        litersToGallons,
        poundsToKilograms,
        kilogramsToPounds,
        gallonsToPounds,
        poundsToGallons,
        gallonsToKilograms,
        kilogramsToGallons,
        litersToPounds,
        poundsToLiters,
        litersToKilograms,
        kilogramsToLiters,
        FuelUnit,
        FuelDensity,
        FUEL_DENSITY
    }
}

// @public
const FUEL_DENSITY: {
    readonly '100LL': FuelDensity;
    readonly 'Jet A': FuelDensity;
    readonly 'Jet A-1': FuelDensity;
    readonly 'Jet B': FuelDensity;
};

// @public
type FuelDensity = {
    readonly kgPerL: number;
} | {
    readonly lbPerGal: number;
};

// @public
type FuelUnit = 'gal' | 'L' | 'lb' | 'kg';

// @public
function gallonsToKilograms(gallons: number, density: FuelDensity): number;

// @public
function gallonsToLiters(gallons: number): number;

// @public
function gallonsToPounds(gallons: number, density: FuelDensity): number;

// @public
function hectopascalsToInchesOfMercury(hectopascals: number): number;

// @public
function hectopascalsToKilopascals(hectopascals: number): number;

// @public
function hectopascalsToMillibars(hectopascals: number): number;

// @public
function hectopascalsToMillimetersOfMercury(hectopascals: number): number;

// @public
function inchesOfMercuryToHectopascals(inchesOfMercury: number): number;

// @public
function inchesOfMercuryToKilopascals(inchesOfMercury: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/units" does not have an export "inchesOfMercuryToHectopascals"
//
// @public
function inchesOfMercuryToMillibars(inchesOfMercury: number): number;

// @public
function inchesOfMercuryToMillimetersOfMercury(inchesOfMercury: number): number;

declare namespace isa {
    export {
        isaTemperatureCelsius,
        pressureAtAltitudeHectopascals,
        densityAltitudeFt,
        speedOfSoundKt,
        trueAirspeedFromCalibratedKt,
        machFromTrueAirspeedKt,
        trueAirspeedFromMachKt,
        ISA_SEA_LEVEL_TEMP_K,
        ISA_SEA_LEVEL_TEMP_C,
        ISA_SEA_LEVEL_PRESSURE_HPA,
        ISA_SEA_LEVEL_PRESSURE_INHG,
        ISA_SEA_LEVEL_DENSITY_KGM3,
        ISA_LAPSE_RATE_K_PER_M,
        ISA_TROPOPAUSE_ALTITUDE_FT,
        ISA_TROPOPAUSE_ALTITUDE_M,
        ISA_STRATOSPHERE_TEMP_K,
        ISA_LAPSE_RATE_C_PER_1000FT,
        ISA_SPEED_OF_SOUND_SEA_LEVEL_KT
    }
}

// @public
const ISA_LAPSE_RATE_C_PER_1000FT = 1.98122;

// @public
const ISA_LAPSE_RATE_K_PER_M = 0.0065;

// @public
const ISA_P0_HPA = 1013.25;

// @public
const ISA_P0_INHG = 29.92126;

// @public
const ISA_SEA_LEVEL_DENSITY_KGM3 = 1.225;

// @public
const ISA_SEA_LEVEL_PRESSURE_HPA = 1013.25;

// @public
const ISA_SEA_LEVEL_PRESSURE_INHG = 29.92126;

// @public
const ISA_SEA_LEVEL_TEMP_C = 15;

// @public
const ISA_SEA_LEVEL_TEMP_K = 288.15;

// @public
const ISA_SPEED_OF_SOUND_SEA_LEVEL_KT = 661.4788;

// @public
const ISA_STRATOSPHERE_TEMP_K = 216.65;

// @public
const ISA_TROPOPAUSE_ALTITUDE_FT = 36089.24;

// @public
const ISA_TROPOPAUSE_ALTITUDE_M = 11000;

// @public
function isaTemperatureCelsius(pressureAltitudeFt: number): number;

// @public
function isFlightLevel(altitudeFt: number): boolean;

// @public
function kelvinToCelsius(kelvin: number): number;

// @public
function kelvinToFahrenheit(kelvin: number): number;

// @public
function kilogramsToGallons(kilograms: number, density: FuelDensity): number;

// @public
function kilogramsToLiters(kilograms: number, density: FuelDensity): number;

// @public
function kilogramsToPounds(kilograms: number): number;

// @public
function kilometersPerHourToKnots(kilometersPerHour: number): number;

// @public
function kilometersPerHourToMetersPerSecond(kilometersPerHour: number): number;

// @public
function kilometersPerHourToMilesPerHour(kilometersPerHour: number): number;

// @public
function kilometersToFeet(kilometers: number): number;

// @public
function kilometersToMeters(kilometers: number): number;

// @public
function kilometersToNauticalMiles(kilometers: number): number;

// @public
function kilometersToStatuteMiles(kilometers: number): number;

// @public
function kilopascalsToHectopascals(kilopascals: number): number;

// @public
function kilopascalsToInchesOfMercury(kilopascals: number): number;

// @public
function kilopascalsToMillibars(kilopascals: number): number;

// @public
function kilopascalsToMillimetersOfMercury(kilopascals: number): number;

// @public
function knotsToKilometersPerHour(knots: number): number;

// @public
function knotsToMetersPerSecond(knots: number): number;

// @public
function knotsToMilesPerHour(knots: number): number;

// @public
function litersToGallons(liters: number): number;

// @public
function litersToKilograms(liters: number, density: FuelDensity): number;

// @public
function litersToPounds(liters: number, density: FuelDensity): number;

// @public
function machFromTrueAirspeedKt(trueAirspeedKt: number, oatCelsius: number): number;

// @public
function metersPerSecondToKilometersPerHour(metersPerSecond: number): number;

// @public
function metersPerSecondToKnots(metersPerSecond: number): number;

// @public
function metersPerSecondToMilesPerHour(metersPerSecond: number): number;

// @public
function metersToFeet(meters: number): number;

// @public
function metersToFeet_2(meters: number): number;

// @public
function metersToKilometers(meters: number): number;

// @public
function metersToNauticalMiles(meters: number): number;

// @public
function metersToStatuteMiles(meters: number): number;

// @public
function milesPerHourToKilometersPerHour(milesPerHour: number): number;

// @public
function milesPerHourToKnots(milesPerHour: number): number;

// @public
function milesPerHourToMetersPerSecond(milesPerHour: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/units" does not have an export "hectopascalsToMillibars"
//
// @public
function millibarsToHectopascals(millibars: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/units" does not have an export "inchesOfMercuryToMillibars"
//
// @public
function millibarsToInchesOfMercury(millibars: number): number;

// @public
function millibarsToKilopascals(millibars: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/units" does not have an export "hectopascalsToMillimetersOfMercury"
//
// @public
function millibarsToMillimetersOfMercury(millibars: number): number;

// @public
function millimetersOfMercuryToHectopascals(millimetersOfMercury: number): number;

// @public
function millimetersOfMercuryToInchesOfMercury(millimetersOfMercury: number): number;

// @public
function millimetersOfMercuryToKilopascals(millimetersOfMercury: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/units" does not have an export "millibarsToMillimetersOfMercury"
//
// @public
function millimetersOfMercuryToMillibars(millimetersOfMercury: number): number;

// @public
function nauticalMilesToFeet(nauticalMiles: number): number;

// @public
function nauticalMilesToKilometers(nauticalMiles: number): number;

// @public
function nauticalMilesToMeters(nauticalMiles: number): number;

// @public
function nauticalMilesToStatuteMiles(nauticalMiles: number): number;

// @public
function poundsToGallons(pounds: number, density: FuelDensity): number;

// @public
function poundsToKilograms(pounds: number): number;

// @public
function poundsToLiters(pounds: number, density: FuelDensity): number;

declare namespace pressure {
    export {
        inchesOfMercuryToHectopascals,
        hectopascalsToInchesOfMercury,
        hectopascalsToMillimetersOfMercury,
        millimetersOfMercuryToHectopascals,
        inchesOfMercuryToMillimetersOfMercury,
        millimetersOfMercuryToInchesOfMercury,
        hectopascalsToKilopascals,
        kilopascalsToHectopascals,
        hectopascalsToMillibars,
        millibarsToHectopascals,
        inchesOfMercuryToKilopascals,
        kilopascalsToInchesOfMercury,
        inchesOfMercuryToMillibars,
        millibarsToInchesOfMercury,
        kilopascalsToMillibars,
        millibarsToKilopascals,
        millibarsToMillimetersOfMercury,
        millimetersOfMercuryToMillibars,
        kilopascalsToMillimetersOfMercury,
        millimetersOfMercuryToKilopascals,
        qnhToQfe,
        qfeToQnh,
        pressureAltitudeFt,
        PressureUnit,
        ISA_P0_HPA,
        ISA_P0_INHG
    }
}

// @public
function pressureAltitudeFt(indicatedAltitudeFt: number, qnhInHg: number): number;

// @public
function pressureAtAltitudeHectopascals(altitudeFt: number, seaLevelPressureHpa?: number): number;

// @public
type PressureUnit = 'inHg' | 'hPa' | 'mmHg' | 'mb' | 'kPa';

// @public
function qfeToQnh(qfeHpa: number, airfieldElevationFt: number): number;

// @public
function qnhToQfe(qnhHpa: number, airfieldElevationFt: number): number;

// @public
function radiansToDegrees(radians: number): number;

declare namespace speed {
    export {
        knotsToKilometersPerHour,
        kilometersPerHourToKnots,
        knotsToMilesPerHour,
        milesPerHourToKnots,
        knotsToMetersPerSecond,
        metersPerSecondToKnots,
        kilometersPerHourToMilesPerHour,
        milesPerHourToKilometersPerHour,
        kilometersPerHourToMetersPerSecond,
        metersPerSecondToKilometersPerHour,
        milesPerHourToMetersPerSecond,
        metersPerSecondToMilesPerHour,
        SpeedUnit
    }
}

// @public
function speedOfSoundKt(temperatureCelsius: number): number;

// @public
type SpeedUnit = 'kt' | 'km/h' | 'mph' | 'm/s';

// @public
function statuteMilesToFeet(statuteMiles: number): number;

// @public
function statuteMilesToKilometers(statuteMiles: number): number;

// @public
function statuteMilesToMeters(statuteMiles: number): number;

// @public
function statuteMilesToNauticalMiles(statuteMiles: number): number;

declare namespace temperature {
    export {
        celsiusToFahrenheit,
        fahrenheitToCelsius,
        celsiusToKelvin,
        kelvinToCelsius,
        fahrenheitToKelvin,
        kelvinToFahrenheit,
        TemperatureUnit
    }
}

// @public
type TemperatureUnit = 'C' | 'F' | 'K';

// Warning: (tsdoc-malformed-html-name) Invalid HTML element: A space is not allowed here
//
// @public
function trueAirspeedFromCalibratedKt(calibratedAirspeedKt: number, pressureAltitudeFt: number, oatCelsius?: number): number;

// @public
function trueAirspeedFromMachKt(mach: number, oatCelsius: number): number;

```
