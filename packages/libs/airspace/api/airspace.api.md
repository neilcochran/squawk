## API Report File for "@squawk/airspace"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AirspaceFeature } from '@squawk/types';
import type { AirspaceType } from '@squawk/types';
import type { ArtccStratum } from '@squawk/types';
import { BoundingBox } from '@squawk/geo';
import type { FeatureCollection } from 'geojson';
import { MatchRange } from '@squawk/search';

// @public
export interface AirspaceByIdentifierOptions {
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "types"
    includeArtcc?: boolean;
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "includeArtcc"
    types?: ReadonlySet<AirspaceType>;
}

// @public
export interface AirspaceCentroidQuery {
    lat: number;
    lon: number;
    toleranceDeg?: number;
}

// @public
export interface AirspaceQuery {
    altitudeFt: number;
    lat: number;
    lon: number;
    types?: ReadonlySet<AirspaceType>;
}

// @public
export interface AirspaceResolver {
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "byArtcc"
    byAirport(identifier: string, types?: ReadonlySet<AirspaceType>): AirspaceFeature[];
    byArtcc(identifier: string, stratum?: ArtccStratum): AirspaceFeature[];
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "byIdentifier"
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "byAirport"
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "byArtcc"
    byCentroid(query: AirspaceCentroidQuery): AirspaceFeature[];
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "byAirport"
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/airspace" does not have an export "byArtcc"
    byIdentifier(identifier: string, options?: AirspaceByIdentifierOptions): AirspaceFeature[];
    forEachIndexed(callback: (feature: AirspaceFeature, ring: readonly number[][], boundingBox: BoundingBox) => void): void;
    query(query: AirspaceQuery): AirspaceFeature[];
    search(query: AirspaceSearchQuery): AirspaceSearchResult[];
    withinBbox(bbox: BoundingBox): AirspaceFeature[];
}

// @public
export interface AirspaceResolverOptions {
    data: FeatureCollection;
}

// @public
export type AirspaceSearchField = 'identifier' | 'name';

// @public
export interface AirspaceSearchQuery {
    limit?: number;
    minScore?: number;
    text: string;
    types?: ReadonlySet<AirspaceType>;
}

// @public
export interface AirspaceSearchResult {
    feature: AirspaceFeature;
    matchedField: AirspaceSearchField;
    ranges: MatchRange[];
    score: number;
}

// @public
export function createAirspaceResolver(options: AirspaceResolverOptions): AirspaceResolver;

export { MatchRange }

```
