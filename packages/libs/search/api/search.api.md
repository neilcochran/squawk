## API Report File for "@squawk/search"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export interface FuzzyMatch<T, F extends string = string> {
    field: F;
    item: T;
    ranges: MatchRange[];
    score: number;
}

// @public
export interface FuzzyScore {
    ranges: MatchRange[];
    score: number;
}

// @public
export function fuzzyScore(query: string, candidate: string): FuzzyScore;

// @public
export function fuzzySearch<T, F extends string = string>(items: readonly T[], query: string, options: FuzzySearchOptions<T, F>): FuzzyMatch<T, F>[];

// @public
export interface FuzzySearchOptions<T, F extends string = string> {
    filter?: (item: T) => boolean;
    keys: (item: T) => readonly SearchField<F>[];
    limit?: number;
    minScore?: number;
}

// @public
export interface MatchRange {
    end: number;
    start: number;
}

// @public
export interface SearchField<F extends string = string> {
    name: F;
    text: string;
}

```
