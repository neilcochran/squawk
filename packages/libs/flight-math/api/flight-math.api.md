## API Report File for "@squawk/flight-math"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

declare namespace airspeed {
    export {
        calibratedAirspeedFromTrueAirspeed
    }
}

declare namespace atmosphere {
    export {
        densityAltitude,
        trueAltitude
    }
}

// Warning: (tsdoc-malformed-html-name) Invalid HTML element: A space is not allowed here
//
// @public
function calibratedAirspeedFromTrueAirspeed(trueAirspeedKt: number, pressureAltitudeFt: number, oatCelsius?: number): number;

// @public
function computeSolarTimes(lat: number, lon: number, date: Date): SolarTimes;

// @public
function correctionAngle(offCourseDistanceNm: number, distanceFlownNm: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/flight-math" does not have an export "headwindCrosswind"
//
// @public
function crosswindComponent(windDirectionDeg: number, windSpeedKt: number, runwayHeadingDeg: number): number;

// @public
function dateToDecimalYear(date: Date): number;

// @public
function densityAltitude(fieldElevationFt: number, altimeterSettingInHg: number, oatCelsius: number): number;

declare namespace descent {
    export {
        topOfDescent,
        topOfDescentFromRate,
        requiredDescentRate,
        requiredClimbRate,
        verticalSpeedToGradient,
        gradientToVerticalSpeed,
        visualDescentPoint
    }
}

// @public
function dmeArcLeadRadial(arcRadiusNm: number, trueAirspeedKt: number, bankAngleDeg?: number): number;

// @public
function endurance(fuelAvailable: number, fuelBurnPerHr: number): number;

// @public
function enduranceDistanceNm(fuelAvailable: number, fuelBurnPerHr: number, groundSpeedKt: number): number;

// @public
function equalTimePoint(totalDistanceNm: number, groundSpeedOutKt: number, groundSpeedBackKt: number): PlanningPoint;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/flight-math" does not have an export "solveWindTriangle"
//
// @public
function findWind(groundSpeedKt: number, trueAirspeedKt: number, trueHeadingDeg: number, trueTrackDeg: number): WindVector;

// @public
function fuelRequired(distanceNm: number, groundSpeedKt: number, fuelBurnPerHr: number): number;

declare namespace glide {
    export {
        glideDistance,
        glideDistanceWithWind
    }
}

// @public
function glideDistance(altitudeAglFt: number, glideRatio: number): number;

// @public
function glideDistanceWithWind(altitudeAglFt: number, glideRatio: number, bestGlideTasKt: number, headwindKt: number): number;

// @public
function gradientToVerticalSpeed(gradientDeg: number, groundSpeedKt: number): number;

// @public
function headwindCrosswind(windDirectionDeg: number, windSpeedKt: number, headingDeg: number): WindComponents;

// @public
function holdingPatternEntry(inboundCourseDeg: number, headingToFixDeg: number, rightTurns?: boolean): HoldingPatternEntryType;

// @public
export type HoldingPatternEntryType = 'direct' | 'teardrop' | 'parallel';

// @public
function isDaytime(lat: number, lon: number, dateTime: Date): boolean;

// @public
function loadFactor(bankAngleDeg: number): number;

declare namespace magnetic {
    export {
        magneticDeclination,
        magneticField,
        trueToMagnetic,
        magneticToTrue,
        dateToDecimalYear,
        WMM_EPOCH,
        WMM_VALID_START,
        WMM_VALID_END
    }
}

// @public
function magneticDeclination(latDeg: number, lonDeg: number, options?: MagneticFieldOptions): number;

// @public
function magneticField(latDeg: number, lonDeg: number, options?: MagneticFieldOptions): MagneticFieldResult;

// @public
export interface MagneticFieldOptions {
    readonly altitudeFt?: number | undefined;
    readonly date?: Date | undefined;
    // Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/flight-math" does not have an export "date"
    readonly decimalYear?: number | undefined;
}

// @public
export interface MagneticFieldResult {
    readonly decimalYear: number;
    readonly declinationDeg: number;
    readonly downIntensityNt: number;
    readonly eastIntensityNt: number;
    readonly horizontalIntensityNt: number;
    readonly inclinationDeg: number;
    readonly northIntensityNt: number;
    readonly totalIntensityNt: number;
}

// @public
function magneticToTrue(magneticBearingDeg: number, latDeg: number, lonDeg: number, options?: MagneticFieldOptions): number;

declare namespace navigation {
    export {
        holdingPatternEntry,
        dmeArcLeadRadial,
        correctionAngle,
        offCourseDistance
    }
}

// @public
function offCourseDistance(trackErrorDeg: number, distanceFlownNm: number): number;

declare namespace pivotal {
    export {
        pivotalAltitude
    }
}

// @public
function pivotalAltitude(groundSpeedKt: number): number;

declare namespace planning {
    export {
        fuelRequired,
        endurance,
        enduranceDistanceNm,
        pointOfNoReturn,
        equalTimePoint
    }
}

// Warning: (ae-unresolved-link) The @link reference could not be resolved: This type of declaration is not supported yet by the resolver
// Warning: (ae-unresolved-link) The @link reference could not be resolved: This type of declaration is not supported yet by the resolver
//
// @public
export interface PlanningPoint {
    distanceNm: number;
    timeHrs: number;
}

// @public
function pointOfNoReturn(fuelAvailable: number, fuelBurnPerHr: number, groundSpeedOutKt: number, groundSpeedBackKt: number): PlanningPoint;

// @public
function requiredClimbRate(distanceNm: number, currentAltitudeFt: number, targetAltitudeFt: number, groundSpeedKt: number): number;

// @public
function requiredDescentRate(distanceNm: number, currentAltitudeFt: number, targetAltitudeFt: number, groundSpeedKt: number): number;

declare namespace solar {
    export {
        computeSolarTimes,
        isDaytime
    }
}

// @public
export interface SolarTimes {
    civilTwilightBegin?: Date;
    civilTwilightEnd?: Date;
    sunrise?: Date;
    sunset?: Date;
}

// @public
function solveWindTriangle(trueAirspeedKt: number, trueCourseDeg: number, windDirectionDeg: number, windSpeedKt: number): WindTriangleResult;

// @public
function standardRateBankAngle(trueAirspeedKt: number): number;

// @public
function standardRateTurnRadius(trueAirspeedKt: number): number;

// @public
function timeToTurn(turnDeg: number, turnRateDegPerSec?: number): number;

// @public
function topOfDescent(currentAltitudeFt: number, targetAltitudeFt: number, descentAngleDeg: number): number;

// @public
function topOfDescentFromRate(currentAltitudeFt: number, targetAltitudeFt: number, descentRateFtPerMin: number, groundSpeedKt: number): number;

// @public
function trueAltitude(indicatedAltitudeFt: number, altimeterSettingInHg: number, oatCelsius: number, stationElevationFt?: number): number;

// @public
function trueToMagnetic(trueBearingDeg: number, latDeg: number, lonDeg: number, options?: MagneticFieldOptions): number;

declare namespace turn {
    export {
        standardRateBankAngle,
        turnRadius,
        standardRateTurnRadius,
        timeToTurn,
        loadFactor
    }
}

// @public
function turnRadius(trueAirspeedKt: number, bankAngleDeg: number): number;

// @public
function verticalSpeedToGradient(verticalSpeedFtPerMin: number, groundSpeedKt: number): number;

// @public
function visualDescentPoint(glidepathAngleDeg: number, thresholdCrossingHeightFt: number): number;

declare namespace wind {
    export {
        solveWindTriangle,
        headwindCrosswind,
        findWind,
        crosswindComponent
    }
}

// @public
export interface WindComponents {
    crosswindKt: number;
    headwindKt: number;
}

// @public
export interface WindTriangleResult {
    groundSpeedKt: number;
    trueHeadingDeg: number;
    windCorrectionAngleDeg: number;
}

// @public
export interface WindVector {
    directionDeg: number;
    speedKt: number;
}

// @public
const WMM_EPOCH = 2025;

// @public
const WMM_VALID_END = 2030;

// @public
const WMM_VALID_START = 2025;

```
