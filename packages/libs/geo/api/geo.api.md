## API Report File for "@squawk/geo"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { Coordinates } from '@squawk/types';
import type { Polygon } from 'geojson';

// @public
function bearing(lat1: number, lon1: number, lat2: number, lon2: number): number;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/geo" does not have an export "bearingAndDistance"
//
// @public
export interface BearingAndDistance {
    bearingDeg: number;
    distanceNm: number;
}

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/geo" does not have an export "bearing"
// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/geo" does not have an export "distanceNm"
//
// @public
function bearingAndDistance(lat1: number, lon1: number, lat2: number, lon2: number): BearingAndDistance;

// @public
export interface BoundingBox {
    maxLat: number;
    maxLon: number;
    minLat: number;
    minLon: number;
}

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/geo" does not have an export "pointInBoundingBox"
//
// @public
function boundingBox(ring: number[][]): BoundingBox;

// @public
function boundingBoxesOverlap(a: BoundingBox, b: BoundingBox): boolean;

// @public
function destination(lat: number, lon: number, bearingDeg: number, travelDistanceNm: number): Coordinates;

// @public
function distanceNm(lat1: number, lon1: number, lat2: number, lon2: number): number;

declare namespace greatCircle {
    export {
        distanceNm,
        bearing,
        bearingAndDistance,
        midpoint,
        destination,
        BearingAndDistance
    }
}

// @public
function midpoint(lat1: number, lon1: number, lat2: number, lon2: number): Coordinates;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/geo" does not have an export "pointInPolygon"
//
// @public
function pointInBoundingBox(lon: number, lat: number, box: BoundingBox): boolean;

// Warning: (ae-unresolved-link) The @link reference could not be resolved: This type of declaration is not supported yet by the resolver
//
// @public
function pointInBoundingBox_2(point: readonly [number, number], bbox: BoundingBox): boolean;

// @public
function pointInPolygon(x: number, y: number, ring: number[][]): boolean;

// @public
function pointInPolygon_2(point: readonly [number, number], polygon: Polygon): boolean;

declare namespace polygon {
    export {
        pointInPolygon,
        boundingBox,
        pointInBoundingBox,
        BoundingBox
    }
}

// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@squawk/geo" does not have an export "pointInBoundingBox"
// Warning: (ae-unresolved-link) The @link reference could not be resolved: This type of declaration is not supported yet by the resolver
//
// @public
function polygonBoundingBox(polygon: Polygon): BoundingBox;

// @public
function polygonCentroid(polygon: Polygon): [number, number] | undefined;

declare namespace polygonGeoJson {
    export {
        pointInPolygon_2 as pointInPolygon,
        polygonCentroid,
        polygonBoundingBox,
        polygonsIdentical,
        polygonsSubstantiallyOverlap,
        boundingBoxesOverlap,
        pointInBoundingBox_2 as pointInBoundingBox
    }
}

// @public
function polygonsIdentical(a: Polygon, b: Polygon): boolean;

// @public
function polygonsSubstantiallyOverlap(a: Polygon, b: Polygon, aCentroid?: readonly [number, number]): boolean;

// Warnings were encountered during analysis:
//
// src/index.ts:3:4 - (tsdoc-characters-after-block-tag) The token "@squawk" looks like a TSDoc tag but contains an invalid character "/"; if it is not a tag, use a backslash to escape the "@"

```
