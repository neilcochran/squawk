{
  "name": "@squawk/build-airway-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Internal build script: processes FAA NASR airway data into the airway dataset",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test 'dist/**/*.spec.js'",
    "test:coverage": "mkdir -p coverage && node --test --experimental-test-coverage --test-coverage-include='dist/**' --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info 'dist/**/*.spec.js'",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@squawk/build-shared": "*",
    "@squawk/types": "*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
