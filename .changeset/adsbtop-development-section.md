---
'@squawk/adsbtop': patch
---

### Added

- A `Development` section in the README, covering how to build and run the CLI out of `dist/`, why the dashboard wants a terminal of its own, and why running it through a pipe or a task runner exercises neither column auto-fit nor row windowing.
