{
  "$schema": "https://turbo.build/schema.json",
  "concurrency": "50%",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build", "^build"]
    },
    "test:coverage": {
      "dependsOn": ["build", "^build"],
      "outputs": ["coverage/**"]
    },
    "check-coverage": {
      "dependsOn": ["test:coverage"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint:pack": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "api:check": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "api:report": {
      "dependsOn": ["build"],
      "outputs": ["api/**"],
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
